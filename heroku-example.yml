setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
#    worker: worker/Dockerfile
  config:
    RAILS_ENV: development
    FOO: bar
run:
  web: uvicorn app.main:app --reload --host 0.0.0.0 --port $PORT
#release:
#  command:
#    - ./deployment-task.sh
#  image: worker